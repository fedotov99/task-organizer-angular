import {Task} from "./task";
import {Subordinate} from "./subordinate";

export class Manager {
  userID: string;
  name: string;
  // Maybe we shouldn't store it there? Can we instead use backend API?
/*  localUserTaskList: Task[];
  subordinateList: Subordinate[];
  uncheckedTasksList: Task[];*/
}
